// @pass
// if/elif/else all return on every path — valid

def classify(x: int) -> int {
    if x > 0 {
        return 1;
    } elif x == 0 {
        return 0;
    } else {
        return -1;
    }
}
