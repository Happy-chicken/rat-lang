// @pass
// trait implementation — all methods present

trait Drawable {
    decl draw() -> int;
}

class Circle {
    let r: int = 1;
}

impl Drawable for Circle {
    def draw(self: Circle) -> int {
        return self.r;
    }
}

def main()->int {
    let c = Circle(5);
    return c.draw();
}
